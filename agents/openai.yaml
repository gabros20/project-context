interface:
  display_name: "Project Context"
  short_description: "Shared append-only project memory for coding-agent sessions"
  default_prompt: "Use project-context to retrieve the relevant durable project state before continuing this repository task."
policy:
  allow_implicit_invocation: true
